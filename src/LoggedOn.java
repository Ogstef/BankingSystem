/* public class LoggedOn extends FileToUser {

    public int whoIsOnline (String fname, String lname){
        int id = 0;
        for (User b : readUsersFromFile("C:\\Users\\sdavg\\OneDrive\\Desktop\\OOP_project\\Users.csv"))
        {
            if (fname.equals(b.getFirstName()) && fname.equals(b.getLastName()) )
            {
                 id = b.getAccountId();
            }
        }
        return id;
    }
}
\*

 */